import 'package:flutter/material.dart';

class SuratDrawer extends StatelessWidget {
  const SuratDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Drawer();
  }
}
